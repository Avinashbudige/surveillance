[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "surveillance-platform"
version = "0.1.0"
description = "AI surveillance platform"
requires-python = ">=3.10"
